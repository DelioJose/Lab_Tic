#include <stdio.h>
	int main(){
		
		float sam,kw,pkw,ppago,vg;
		
		printf("Digite o salario minimo:");
		scanf("%f", &sam);
		
		printf("Digite a quantidade de quilowatts consumida por uma residência:");
		scanf("%f", kw);
		
		pkw = sam / 5.0;
		pre_pago = pkw * kw;
		vp = ppago - ( ppago * ( 15 / 100.0 ));
	
	return 0;
}
